{% extends "layouts/main.nunj" %}


{% block content %}

    <div class="container">
        <h1>{{ page.title }}</h1>

        {#<p class="secondary">moment(post.created).format("dddd, MMMM, YYYY")</p>#}
        <p class="secondary">{{ page.created }}</p>

        {{ contentForRegionName("body", page) }}
    </div>

{% endblock %}
